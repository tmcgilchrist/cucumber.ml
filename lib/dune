(library
 (name cucumber)
 (public_name cucumber)
 (libraries cmdliner re re.perl str sedlex menhirLib unix)
 (flags
  (:standard -w -42-32))
 (preprocess
  (pps sedlex.ppx))
 (modules
  effects
  gherkin_ast
  gherkin_keywords
  gherkin_parser
  lex
  parser
  pickle
  step
  step_registry
  tag
  table
  location
  docstring
  dialect
  color
  lib
  report
  outcome
  cucumber))

(menhir
 (modules parser)
 (flags --table))
